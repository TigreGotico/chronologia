# Benchmarking chronologia against other date parsers

This page is for anyone who has never touched this benchmark before. It
explains, in plain language: what gets measured, why, how to run it
yourself, and what the numbers do *not* tell you.

## What "the benchmark" even is

chronologia is one of several Python libraries that turn a sentence like
`"in three weeks"` or `"le 5 juin 2027"` into a computer-usable date. Two
well-known alternatives are:

- [`dateparser`](https://dateparser.readthedocs.io/) — a general-purpose,
  many-language date-string parser;
- [`python-dateutil`](https://dateutil.readthedocs.io/)'s `parser.parse` —
  a flexible parser mostly aimed at English-shaped date *strings*
  (`"2027-06-05"`, `"June 5, 2027"`), with only limited fuzzy/free-text
  support.

The benchmark in `benchmark/` runs all three engines over the **same**
set of natural-language sentences and checks whether each engine's answer
matches a human-derived "gold" answer. It reports one thing only: **how
often each engine gets the exact date right**, broken down by language.

This is a *differential* benchmark, not a certification: nothing here
blocks a merge or fails CI. The project's house rule is "benchmark, don't
assert" — we measure and report honestly, including cases where a
competitor wins, rather than writing a test that insists chronologia must
win.

## Where the gold answers come from

The gold set is **not** invented for this benchmark. It is pulled live,
every time you run it, from chronologia's own natural-language test
corpora under `test/nl_corpus_<lang>/` — the hand-written `(sentence,
expected_date)` pairs that the project's own test suite already asserts.
Those expectations were derived by a human doing independent date
arithmetic (see each corpus's `_corpus.py` module docstring), not by
running any of the three engines and copying their output. That
independence is what makes the comparison meaningful: nobody is grading
an engine against its own answer key.

That said, the corpus is still chronologia's own — the sentences were
written to exercise the phrasings chronologia set out to read, not sampled
independently of it — so the comparison is self-selected in chronologia's
favour, and the score should be read as "how chronologia does on the
phrasings it was built for," not a neutral, third-party benchmark.

`benchmark/adapter.py` does the pulling. It uses pytest's own collection
machinery to walk every `test/nl_corpus_*/` package and gather every
parametrized test case shaped `(text, expected)` where `expected` is a
real `date`/`datetime` object (as opposed to, say, `extract_duration`'s
`(text, expected_timedelta)` cases, which this benchmark is not about and
which the type check naturally filters out). No language is special-cased
by name — add a new `nl_corpus_xx/` package to the test suite and the
benchmark picks its cases up automatically next run.

This pulls every gold case from the corpora that use the
`text,expected` convention (corpora that only use other parametrize
shapes, such as semantic-parity `(xx_text, en_text)` pairs, are not yet
walked by this adapter — see Caveats). The count grows automatically as
corpora are added.

## How scoring works

Every gold case is `(language, sentence, expected_date, anchor_datetime)`.
`anchor_datetime` is the corpus's fixed "now" (`ANCHOR`, a specific
Tuesday at 13:04) that relative phrases like "in three weeks" resolve
against, so results are 100% reproducible — no wall-clock dependency.

For each case, each engine gets exactly one of three outcomes:

- **exact** — the engine's answer's *date* equals the gold date;
- **no-parse** — the engine returned nothing at all;
- **wrong** — the engine returned some date, but it isn't the gold date.

`dateparser` and `dateutil` both return a plain `datetime`, collapsed to
a single instant. chronologia's `extract_timespan` instead returns a
`DateSpan` — the whole width of time a phrase names (`"june 2027"` is a
month wide, not a single second). Per the project's house rule,
chronologia is graded on `span.start.date()` so the three engines are
compared on the same axis; this necessarily *understates* what
chronologia gives you beyond that (the span's end, its resolution tag,
non-Gregorian calendar metadata, and so on), since the competitors have
no equivalent to credit at all.

## Running it yourself

```bash
# full run over every corpus (takes well under a minute; mostly pytest
# collection overhead, not engine time)
python benchmark/run.py --out benchmark/SCOREBOARD.md

# restrict to a few languages while iterating
python benchmark/run.py --langs en de ar

# fast, deterministic smoke sample (what the committed test runs)
python benchmark/run.py --sample 50 --seed 0
```

`benchmark/SCOREBOARD.md` in this repo is a **committed snapshot** from
the last full run — regenerate it with the first command above whenever
you want a fresh number; it is not auto-regenerated by CI.

## Honest caveats

- **This is not a certification.** A benchmark score is a snapshot of one
  gold set on one day; it says nothing about phrases nobody has written a
  test for yet.
- **The playing field isn't perfectly level.** dateparser and dateutil are
  general-purpose parsers not designed around chronologia's span-native
  contract or its non-Gregorian/eras/recurrence features; they are being
  asked to do a narrower job than they're built for (a single left-edge
  date) and are graded only on that narrower job.
- **Business-day cases are graded without jurisdiction context.**
  chronologia's `extract_timespan` accepts an optional `jurisdiction` (an
  ISO country code) to make "in 3 working days" holiday-aware; the
  benchmark calls it without one, so a handful of German
  `test_nl_business_days.py` cases whose gold values assume a specific
  jurisdiction's holiday calendar score as "wrong" here even though
  chronologia's own test suite (which does pass the jurisdiction) passes.
  This is a known adapter limitation, not an engine bug.
- **Not every corpus is walked yet.** Corpora that only carry
  semantic-parity blocks (`(xx_text, en_text)` parametrize shape) rather
  than `(text, expected_date)` cases are not currently pulled into the
  gold set; languages with zero rows in the scoreboard simply have no
  `text,expected`-shaped cases yet, not zero support.
- **Where a competitor wins, the scoreboard says so** — see the "Honest
  notes" section at the bottom of `benchmark/SCOREBOARD.md`. As of the
  current snapshot chronologia leads or ties on every language covered;
  that is expected to change as either the gold set grows or a competitor
  library improves, and the scoreboard will report it plainly when it
  does.

## Related projects

See the [chronologia README](../README.md) for the project's own
description and links to sibling repos in the org.
